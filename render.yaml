services:
  - type: web
    name: bingo-musical
    runtime: node
    plan: free
    region: oregon
    buildCommand: npm install && npm run build
    startCommand: npm start
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: "20.18.0"
      # Obligatorio en el build: Vite embebe la key en el frontend
      - key: VITE_YOUTUBE_API_KEY
        sync: false
